{
  "name": "i-dont-know-what-to-eat",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"require('fs').writeFileSync('config.js', 'const CONFIG = { GOOGLE_MAPS_API_KEY: \\\"' + (process.env.GOOGLE_MAPS_API_KEY || '') + '\\\" };');\""
  }
}
